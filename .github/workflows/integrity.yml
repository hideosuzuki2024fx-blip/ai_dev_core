name: Integrity Check
on:
  push:
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Integrity Check
        shell: pwsh
        run: pwsh ops/scripts/check_integrity.ps1