name: Integrity Check

on:
  push:
  pull_request:

jobs:
  verify:
    name: Code Integrity Verification
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Integrity Check (Code Only)
        shell: pwsh
        run: |
          Write-Host '🧩 Running Integrity Check (code files only)...'
          pwsh ops/scripts/check_integrity.ps1